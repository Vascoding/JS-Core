{{> header}}
<div class="content">
    <section id="viewCatalog">
        {{> navigate}}
        <div class="posts">
            {{#each posts}}
                    {{> post}}
                    {{else}}
                    <p>No posts in database!</p>
                {{/each}}
        </div>
    </section>
</div>
{{> footer}}